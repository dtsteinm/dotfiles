# User-defined aliases
alias apt-install='sudo apt-get install'
alias apt-search='aptitude search'
alias apt-show='aptitude show'
alias apt-remove='sudo apt-get remove'
alias cp='cp -iv'
alias diff='colordiff'
alias echo-path='echo -e ${PATH//:/\\n}'
alias grep='grep --color=auto'
alias info='info --vi-keys'
alias lh='ls -a | egrep "^\."'
alias ll='ls -Ahl'
alias ls='ls --color=auto'
alias matrix='cmatrix -b -u 3 -C cyan'
alias mkderp='mkdir -p' # Herpa
alias mp3gain='mp3gain -s i -p'
alias mv='mv -iv'
alias rename='rename -v'
alias reverse='grep -v'
alias rm='rm -Iv' 
alias rmderp='rmdir -p' # Herpa
alias svim='sudoedit'

# Probably better if I properly configure PAM...
#alias stop-forkbomb='while (sleep 100 &) do; done'

#alias apt-clean='sudo apt-get autoremove && sudo apt-get autoclean'
#alias apt-upgrade='sudo apt-get update && sudo aptitude safe-upgrade'
#alias deleted='lsof +L1'
#alias doit='screen -p 1 -d -R'
#alias fix-keyring='/usr/bin/screen -X GNOME_KEYRING_CONTROL $GNOME_KEYRING_CONTROL'
#alias fix-keyring='/usr/bin/screen -X setenv SSH_AGENT_PID $SSH_AGENT_PID SSH_AUTH_SOCK $SSH_AUTH_SOCK'
#alias irssi='screen -S irc irssi'
#alias piano='screen -S piano pianobar'
#alias screen='/usr/bin/screen -S etc -c ~/.screen/startup'
#alias spc='scp -p'
#alias svi='sudo vi'

# Global aliases
alias -g gp='| grep -i'

# Suffix alias
alias -s py=vim
alias -s txt=vim
alias -s log="less -MN"
alias -s html=chromium
alias -s mkv=mplayer
alias -s avi=mplayer
